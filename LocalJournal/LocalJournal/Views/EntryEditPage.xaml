<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LocalJournal.Views.EntryEditPage"
             Title="Edit Entry">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" Priority="0" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="20" Padding="15">
                <Entry Text="{Binding Entry.Title}" Placeholder="Title" d:Text="Title" FontSize="Small"
                       Keyboard="Chat"/>

                <StackLayout Orientation="Horizontal" Spacing="10">
                    <Label Text="Encrypt" VerticalOptions="Center" />
                    <Switch IsToggled="{Binding Entry.Encrypted}" />
                </StackLayout>

                <Editor Text="{Binding Entry.Body}" Placeholder="Body" d:Text="Entry body" FontSize="Small" Margin="0"
                        Keyboard="Chat" AutoSize="TextChanges" HeightRequest="500"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>
